const { GuildMember, Client, EmbedBuilder } = require('discord.js')
const { join_channel } = require('../../../config.json')

module.exports = {
    // name: "guildMemberRemove",
    // once: false,
    // /**
    //  * @param {GuildMember} member 
    //  * @param {Client} client
    //  */
    // execute(member, client) {
        
    //     const channel = client.channels.cache.get(join_channel)

    //     const embed = new EmbedBuilder()
    //                     .setTitle(`Aurevoir ${member.user.username}, merci d'être venu !`)
    //                     .setAuthor({name: member.displayName})
    //                     .setDescription('Nous sommes ravis que tu sois venu.')
    //                     .setThumbnail(member.user.displayAvatarURL({ dynamic: true }))
    //                     .setFooter({text: "Créer par ????"})

    //     channel.send({embeds: [embed]})
            
    // }

}